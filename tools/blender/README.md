# Blender MCP addon

1. Blender > Edit > Preferences > Add-ons > Install
2. Pick `tools/blender/addon.py`
3. Enable **Blender MCP**
4. 3D View N-panel > BlenderMCP > Connect on port **9876**

Cursor already has an MCP entry that runs `uvx blender-mcp` against that port.
